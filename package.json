{
  "name": "poomani-tv-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node api/index.js",
    "install-all": "npm install --prefix frontend && npm install --prefix api"
  }
}